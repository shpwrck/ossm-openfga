apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - gateway.yaml
  - httproutes.yaml
  - reference-grant.yaml
# authpolicies.yaml.tmpl is applied by scripts/install-ingress.sh (envsubst:
# needs the runtime OpenFGA store id). API-key Secrets for alice/bob are
# generated by the script — never committed.
