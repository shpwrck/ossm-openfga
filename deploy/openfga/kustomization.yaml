apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - postgres.yaml
  - bridge.yaml
# The OpenFGA server itself is installed by the official Helm chart
# (scripts/install-openfga.sh) with values.yaml in this directory —
# datastore URI and API token are generated at install time and never
# stored in this repository.
