# A revision tag named `default` lets namespaces enroll with the classic
# `istio-injection: enabled` label (which otherwise only works when the active
# revision itself is named `default`).
apiVersion: sailoperator.io/v1
kind: IstioRevisionTag
metadata:
  name: default
spec:
  targetRef:
    kind: Istio
    name: default
